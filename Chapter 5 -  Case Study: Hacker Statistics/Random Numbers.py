import numpy as np
#rand() from the random package generates a random number between 0 and 1.
random_number = np.random.rand()
print(random_number)

#randint(a, b) from the random package generates a random integer between a and b.
throw_a_dice = np.random.randint(1, 7)
print(throw_a_dice)